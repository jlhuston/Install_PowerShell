param ([switch] $Install, [alias("Upgrade")][switch] $Update, [switch] $Uninstall, [switch] $CheckVersion)
# The switches above are used to control program execution flow and instruct the script on what to do
